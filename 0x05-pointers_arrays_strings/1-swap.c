#include "main.h"
#include "main.h"
#include <stdio.h>

/**
 * swap_int - swap the code
 * @a: swap to b
 * @b: swap to a
 * Return: 0 success
*/
void swap_int(int *a, int *b)
{
int temp = *a;
*a = *b;
*b = temp;
}
