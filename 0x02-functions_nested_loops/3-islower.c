#include "main.h"
/**
 * main - check the code.
 * Description:  checks for lowercase character
 * Return: Always 0.
 */

int _islower(int c)
{
if i(c >= 'a'; && c <= 'z';)
{
return (1);
}
else
{
return (0);
}
}
