#include "main.h"
/**
 * main - check the code
 *print_alphabet - prints the lowercase alphabet
 * Return: Always 0.
 *
 */

void print_alphabet(void)
/*print_alphabet - prints the lowercase alphabet*/
{
char ch;
for (ch = 'a'; ch <= 'z'; ch++)
_putchar(ch);
_putchar('\n');
}
