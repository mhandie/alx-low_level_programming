#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - ls prints all the numbers of base 16 in lowercase
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char ls;
	{
		for (ls = '0'; ls <= '9'; ls++)
		putchar(c);
	}
	{
		for (ls = 'a'; ls <= 'f'; ls++)
		putchar(ls);
	}
	putchar('\n');
	return (0);
}

