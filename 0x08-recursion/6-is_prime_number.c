#include "main.h"
int actual_prime(int n, int i);
/**
 * is_prime_number - integer may be a prime number
 * actual_prime - calculates numbers
 * @n: number
 * @i: iterator
 *
 * Return: 1 if prime number, 0 if not
 */
int is_prime_number(int n)
{
if (n <= 1)
return (0);
}
int actual_prime(int n, int i)
{
if (i == 1)
return (1);
}
